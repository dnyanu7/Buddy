{
    "name": "your-nodejs-project",
    "version": "1.0.0",
    "scripts": {
      "start": "docker run -p 3000:3000 my-docker-app",
      "build": "docker build -t my-docker-app .",
      "deploy": "npm run build && docker push my-docker-repo/my-docker-app"
    },
    "dependencies": {
      "express": "^4.17.1"  
    },
    "devDependencies": {
      "nodemon": "^2.0.7"  
    }
  }
  
